# SQL Server to PostgreSQL Control Flow Conversion

This guide covers the conversion of SQL Server control flow statements to PostgreSQL equivalents.

## IF Statements

### SQL Server:
```sql
IF condition
BEGIN
    -- code block
END
ELSE
BEGIN
    -- code block
END